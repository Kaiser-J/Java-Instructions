int[] mijnArray = {5, 7, 13, 15, 18, 20, 21};
// int[] mijnArray = new int[10];

void setup(){
  // Optie 1 - println(mijnArray);
  // Optie 2
  for (int i  = 0; i < 7; i++){
    println(mijnArray[i]);
  }
}
